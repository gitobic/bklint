[project]
name = "bklint"
version = "0.1.0"
description = "Bookmark linter - clean up and organize browser bookmarks (Firefox & Chrome)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "rich>=13.7.0",
]

[project.scripts]
bklint = "bklint:main"
